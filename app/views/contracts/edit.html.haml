%ul.breadcrumb
  %li= link_to 'Соревнования', champs_path
  %li= link_to @champ.full_title, edit_champ_path(@champ.id)
  %li= @stage.title

  %hr
  .h2= @stage.title


= render partial: 'form', locals: {form_action: champ_stage_path(@champ, @stage)}