%ul.breadcrumb
  %li= link_to 'Соревнования', champs_path
  %li= link_to @champ.full_title, edit_champ_path(@champ.id)
  %li Новая команда

%h3 Добавить команду
%br
= render partial: 'form', locals: {form_action: champ_teams_path(@champ)}